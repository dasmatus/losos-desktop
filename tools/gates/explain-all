#!/usr/bin/env bash
# Compatibility wrapper for tools/gates/Justfile's explain-all recipe.
set -euo pipefail

repo=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../.." && pwd -P)
exec just --justfile "$repo/tools/gates/Justfile" --working-directory "$repo" explain-all
