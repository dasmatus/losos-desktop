project(
  'losos-swap',
  'c',
  version : '0.1.0',
  default_options : ['c_std=c11', 'warning_level=3', 'werror=true'],
)

executable(
  'losos-swap',
  'src/losos-swap.c',
  install : true,
  install_dir : get_option('prefix') / 'lib' / 'losos',
)
